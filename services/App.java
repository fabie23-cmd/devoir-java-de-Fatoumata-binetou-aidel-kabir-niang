import entities.patient;
import entities.RV;
public class App {

    public static void main(String[] args) throws Exception {
    int choix
    do {
        choix=GesRvVue. showMenu();
        GesRvVue.getScanner ().nextLine();
        switch (choix) (
        case 1->{
           service.addrendez-vous(Rendez-vousVue.saisie());
        }
        case 2->{
           service.listerRv();
        }
        case 3->{
           service.listerRendez-vousVue.getTypeRv();
        }
        service. listerRendez-vous(Rendez-vousVue-getTypeRv());
        }
        default ->{
        }

      }
    }while(choix!=6);
  }
}

   
    } while (choix!=6);
    
